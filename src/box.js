import React, {Component} from 'react';
import ReactDOM from 'react-dom';
import ItemSlot from './item-slot.js';

class Box extends Component {
  constructor (props){
    super(props);
  }

  render() {
     return (
       //include shelves
       // render as many shelves as needed
      <div>
      
      </div>
     );
   }
}


export default Box;